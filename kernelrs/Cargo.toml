[package]
name = "kernelrs"
version = "0.1.0"
edition = "2024"
autobins = false

[dependencies]

[lib]
crate-type = ["staticlib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
